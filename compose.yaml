services:
  api:
    build:
      context: .
    image: devops-api:0.1.0
    container_name: devops-api-compose
    ports:
      - "8080:8080"
    environment:
      APP_ENV: development
      APP_VERSION: "0.1.0"
